- [2026-05-01 09:07 EDT] MCP skeleton cleanup: added explicit field-level zod descriptions in bot/mcp-server.js to ensure clean spacing/readability in discovery-rendered schemas. Syntax check passes.
- [2026-05-02] review_read_pending migration verified in core/review.js, telegram-callback-server.js, and bot/mcp-server.js; no code changes required. MCP tool returns structuredContent ok with same DB rows as direct SQL.
- [2026-05-19] Obsidian wiki scaffolding added under `AustinVault/Projects/IG Approval Automation*` with overview, architecture, commands, and MCP migration notes.
- [2026-05-22 13:56 EDT] Recovery UX pass: tightened Telegram recovery/status copy in `bot/telegram-callback-server.js`, added repo-local worker LaunchAgent plist at `run/com.austincaddell.ig-approval-worker.plist`, documented enable/restart commands in `README.md`, and re-ran `npm run validate:recovery` plus plist/syntax validation. No live Instagram actions performed.
- [2026-05-25 00:xx EDT] Single-instance protection landed for the Telegram bot/callback server. Added `bot/single-instance.js` lock helper with stale-lock cleanup, wired `bot/telegram-callback-server.js` to acquire/release `data/telegram-bot.lock` (override via `TELEGRAM_BOT_LOCK_PATH`) before polling/listening, added clearer `EADDRINUSE` shutdown messaging, and documented the lock in `README.md` + `.env.example`. Validation: `node --check bot/telegram-callback-server.js`, `node --check bot/single-instance.js`, and a non-destructive inline lock-file harness covering duplicate-instance rejection + stale-lock recovery.
- [2026-05-25 00:30 EDT] Smoke diagnosis (non-destructive): repo validations all pass (`validate:recovery`, `validate:review-flow`, `validate:challenge`). Live DB shows `AUTOMATION_ENABLED=false` since 2026-05-22 18:52:27 after worker `job_blocked`/`automation_paused` with `CHECKPOINT_DETECTED` pre-click on job 6; worker LaunchAgent is running but only prints `Automation paused`. Queue state: 6 blocked, 2 queued, 0 pending approvals, 1 approved-without-active (candidate 1 drift). Callback server process/lock exist on PID 36035, but local HTTP `/debug/queue` accepts TCP and returns no bytes; `sample` shows the main thread blocked inside `execFileSync`, so bot HTTP responsiveness is currently degraded/hung while waiting on a synchronous child command.
- [2026-05-25 10:05 EDT] Candidate 1 / job 6 cleanup inspection (non-destructive): candidate 1 and candidate 2 are the same canonical reel (`/reel/DW1l10nEYez/`) with different `igsh` query params. Candidate 1 history = blocked job 4 on 2026-04-24 and blocked job 6 on 2026-05-22, both `CHECKPOINT_DETECTED`; job 6 paused automation pre-click and left screenshot artifact `screenshots/job-6-1779475947659.png`. Candidate 2 later succeeded on 2026-05-25 via job 7 against the same reel, so candidate 1’s block looks historically plausible but now stale as a queue blocker. Recommendation: do not requeue candidate 1; resolve remaining live drift via duplicate/canonical-post cleanup rather than another browser attempt. Candidate 3 remains separate dead test data drift.
- [2026-05-25 16:15 EDT] Live callback + drift cleanup pass: confirmed `com.austincaddell.ig-approval-bot` LaunchAgent is the live callback-server path (`npm run bot:start` -> `node bot/telegram-callback-server.js`), local `/debug/queue` is responsive again, and `npm run validate:callback-responsiveness` passes (`async /debug/queue=8ms`). Added non-destructive recovery suppression support via `recovery_suppressions` + `scripts/suppress-recovery-candidate.js`, wired recovery/status queries to ignore suppressed candidates instead of mutating approval/job history, validated with `npm run validate:recovery`, migrated the live DB, and suppressed candidate 1 (`duplicate_canonical_success_same_reel`) plus candidate 3 (`dead_test_candidate_failed_selector`). Live status now reads `approvedWithoutActive=0`, `recoverySuppressedCount=2`, `success=1`, `failed=1`, `blocked=6`. Bot LaunchAgent was kickstarted and is healthy on PID 53235 with lock file ownership updated. Sustained >1 live-job verification was not attempted because it would require new live Instagram actions/browser execution.
- [2026-05-25 18:43 EDT] Real queue-backed live verification outcome confirmed from the live DB/worker state. Two fresh manual candidates were approved by `subagent_live_verification` and entered the normal queue (`candidate 5 https://www.instagram.com/p/DXcMYgCjkJt/`, `candidate 6 https://www.instagram.com/p/DLpWPkcCIih/`; approvals 11/12 at `2026-05-25 20:59:13` UTC). The LaunchAgent worker (`com.austincaddell.ig-approval-worker`, PID 81215 at inspection) claimed them transactionally and ran them as jobs 9 and 10. Outcomes: job 9 failed `LIKE_BUTTON_NOT_FOUND`; job 10 progressed farther and clicked `svg[aria-label="Like"]` but failed `LIKE_STATE_NOT_CONFIRMED`. No `CHECKPOINT_DETECTED` / automation pause occurred on either fresh live job, `AUTOMATION_ENABLED` remained `true`, `/debug/queue` returned no queued jobs afterward, and live totals became `success=1 failed=3 blocked=6 queued=0`. Interpretation: browser-action hardening appears to have improved reliability enough to get past the previous checkpoint-stop mode, but end-to-end success is still not reliable on fresh live jobs because the action/confirmation layer remains brittle.
- [2026-05-25 18:57 EDT] Queue-backed action-layer hardening pass 2 completed. Repro loop used the two fresh failures plus live post inspection. Root causes isolated: (1) selector logic was global and could choose comment-level `Like` controls even when the primary post row was already `Unlike`/liked, and (2) chosen controls were clicked via `locator.nth(index)` from a live interactive-node list that mutates/reorders on Instagram, so the stored index could drift and click the wrong neighbor (observed on candidate 5 where the intended primary row later resolved to a `Reply`/adjacent control during inspection). Changes: `worker/selectors.js` now detects/scopes to the primary action row (requires like/liked + at least two of Comment/Share/Save), reports row state (`liked` vs `unliked`), and resolves the chosen control to a stable element handle via `document.elementFromPoint(center)` instead of relying on a drifting locator index; `worker/run-once.js` now short-circuits success when the primary row is already liked and verifies liked state against the primary row only; `scripts/validate-like-control-selection.js` now covers the liked-row/comment-like interference case. Validation: syntax checks + selector regression passed; no-click live inspections on candidate 5/6 correctly classified candidate 5 as `unliked` and candidate 6 as `liked`; limited queue-backed live re-tests created jobs 11/13. Job 11 (candidate 6) succeeded safely as `already liked` with no extra like action. Job 12 (candidate 5) exposed the locator-index drift bug (`LIKE_STATE_NOT_CONFIRMED`). After the stable-handle patch, job 13 (candidate 5) succeeded end-to-end with click + verification (`Like` -> `Unlike`). Live reliability improved from 0/2 on the fresh queue-backed repro to 2/2 on the limited post-patch re-tests, though one success was an already-liked no-op path. Remaining risk: Instagram DOM/accessible-name churn is still high, so future breakage is most likely around label discovery or action-row structure changes rather than queue/control-plane plumbing.
- [2026-05-25 19:08 EDT] Small fresh normal-flow verification produced one more unseen live success before the hourly safety cap kicked in. Read-only feed probe against the logged-in `.browser-profile` surfaced fresh posts `DYxgQ52D90S`, `DYkn90OiUsa`, and `DYuWhu8MEXJ`; only the first was staged to avoid leaving extra queued work once the cap hit. Commands used: (1) automated feed scrape via Playwright persistent context to collect post URLs, (2) `npm run enqueue -- "https://www.instagram.com/p/DYxgQ52D90S/"`, and (3) a local SQLite transaction mirroring the Telegram approve path to insert approval 13 (`decided_by='subagent_small_fresh_batch'`) and queued job 14. The LaunchAgent worker picked job 14 normally (`queued` -> `running` -> `success`), and `run_events` show a real click/verify path, not an already-liked no-op: `clickedSelector="DIV[aria-label=\"Like\"] ..."`, `verifiedSelector="DIV[aria-label=\"Unlike\"] ..."`, outcome `clicked_and_verified`. Post-state: `/debug/queue` empty, `AUTOMATION_ENABLED=true`, totals `success=4 failed=4 blocked=6`, and `recent successes last hour=3`, so the configured `HOURLY_LIMIT=3` is now the immediate reason to stop instead of staging more same-hour live jobs. Interpretation: the patched action path now looks healthier in ordinary queue-backed execution because it has delivered two consecutive real click+verify successes on fresh/unseen live posts (job 13 retest on candidate 5 and fresh job 14 on candidate 7) plus one safe already-liked success, with no checkpoint pauses since the patch.
- [2026-05-28 23:10 EDT] Phase 2 reliability implementation completed in-code. Added layered selector scoring/layout-family diagnostics (`worker/selectors.js`), page-shape classification (`worker/page-shape.js`), multi-signal post-click verification with narrower verification codes (`worker/verification.js`), and smarter bounded retry behavior wired into `worker/run-once.js`. Failure policy mapping now treats selector/verification failures as retry-now classes, suppresses unavailable targets, and pauses on unsupported shapes. Added/updated validations for selector layout families, page-shape classification, verification classification, worker loop, canary/preflight, automation status, challenge detection, and Telegram result reporting; all passed in repo-local validation.
- [2026-05-26 09:xx EDT] Worker-risk follow-up completed without live Instagram interaction. Diagnosed the remaining post-auto-claim risks in the loop/logging/LaunchAgent path. Implemented timestamped console output for `scripts/run-worker-loop.js` and `worker/run-once.js` via new shared helper `scripts/lib/timestamped-console.js`; LaunchAgent plist now runs `/usr/local/bin/node scripts/run-worker-loop.js` directly instead of `npm run ...` so launchd logs stay cleaner and timestamped. Hardened wake behavior by keeping `fs.watch` as an optimization but adding a polling backup using SQLite `PRAGMA data_version` plus DB/file snapshots (`WORKER_WAKE_POLL_MS`, optional `WORKER_DISABLE_FS_WATCH`) so wakeups still work on weaker/platform-specific watch implementations. Extended `scripts/validate-worker-loop.js` to cover timestamped logs, idle wake, polling-only wake with `fs.watch` disabled, cooldown early wake on fresh DB writes, and the intentional policy that an already-existing backlog still drains at cooldown pace between runs. Updated `README.md` and `.env.example` to document the behavior. Validations run: `node --check scripts/run-worker-loop.js`, `node --check worker/run-once.js`, `plutil -lint run/com.austincaddell.ig-approval-worker.plist`, `npm run validate:worker-loop`, `npm run validate:recovery`.
- [2026-05-26 09:41 EDT] Telegram result reporting seam tightened and validated without live Instagram actions. Extracted terminal job-result cursor/query/format/batching logic out of `bot/telegram-callback-server.js` into new `core/telegram-result-reporting.js`, leaving the bot with a thin poll/send loop. Reporter behavior: first start still snapshots the current latest terminal event id so old history is not backfilled; new `job_success` / `job_failed` / `job_blocked` events are read after the cursor, formatted with candidate/job ids + post URL + compact reason/guidance, and coalesced into as few Telegram sends as possible per poll cycle to reduce spam. Added `scripts/validate-telegram-result-reporting.js` plus package script `validate:telegram-result-reporting`; local validations passed: `node --check core/telegram-result-reporting.js`, `node --check bot/telegram-callback-server.js`, `node --check scripts/validate-telegram-result-reporting.js`, `npm run validate:telegram-result-reporting`, `npm run validate:telegram-intake`, `npm run validate:review-flow`.
